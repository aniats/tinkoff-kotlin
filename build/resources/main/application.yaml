server:
  port: 9000

spring:
  application:
    name: lesson9
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: root
    password: root
  liquibase:
    change-log: classpath:/db/changelog.2022.04/22-01-db.changelog.xml
    enabled: true

# Haven't figured out what service to use
some-client:
  url: google.com
  get-cover-by-name: /search?q={name}

